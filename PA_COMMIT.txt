# The solana-protocol-adapter commit this release pairs with.
# Update on every release; the IDL files and hand-written values are regenerated
# from and reviewed against this commit.
#
# Format: full 40-character commit SHA, one per line. Comments allowed.

# solana-protocol-adapter HEAD (branch: solana/anomapay)
# Tier 1–6 v0.1 integration anchored against this commit.
# Note: PA-side types.rs (SolanaExternalCall import flip) and Cargo.toml dep
# additions are present in the working tree of this commit but not yet
# committed there. v0.1 ships against this commit's behavior with the
# integrator-side migrations applied.
8519d2dc3d33f97cb5ee70c2463f2d3ae862e932

# solana-protocol-adapter main (beta deployment 2026-08-08; program crate renamed
# to protocol-adapter 2026-08-11 — the IDL metadata.name is protocol_adapter)
# Devnet beta PA 9hDoEFv9hyECfruQUxDetE8CiuFrB2fbCiHuD5GUKFeF is deployed and
# initialized from this commit (production build). Predecessor deployments,
# including De5uxTic..., are retired; see the PA repo's
# solana-pa-prototype/docs/DEVNET_DEPLOYMENT.md and docs/INTEGRATION.md.
9ecaa03c43f1b71b8568b0a9bebba88a0b050efd

# solana-protocol-adapter anthony/arm-v2-port (the V2 adapter). Settle takes the
# event-CPI accounts, PAState carries schema_version and kind_table_commitment,
# TransactionExecutedEvent carries is_consumed. Devnet V2 PA
# 28Hvr1YFv2ouGN2fS99aF3ZzYXzkncJVVaHcZNhquLFT was upgraded to this commit on
# 2026-09-16 (production build); the adapter program source is unchanged since
# 2bb6fb5, the previous pin. This commit also carries the V2 SPL token forwarder,
# which this release does NOT pair with yet: the forwarder interface file, id
# and segment builders are updated by the forwarder pairing (anoma/dos-pm#76).
30f13a4800476aac9f87149d0915a6af8fe7e532

# solana-protocol-adapter anthony/anomapay-resource-v2 (the V2 adapter with the
# V2 SPL token forwarder, program 5CrHbBeHjg53UyL3Htn9dCYYTy68fMcrbDoeAdo4yQrx).
# The forwarder's wrap input is 122 bytes without the signature, the wrap and
# unwrap segments are 8 and 7 accounts, and nonce bitmaps are created by the
# permissionless init_nonce_bitmap instruction. idl/spl_token_forwarder.json
# is regenerated from this commit; the adapter program is unchanged since
# 30f13a48, the previous pin.
6fee32a8503300bac36795e84ff1c86a9c9b6c11

# solana-protocol-adapter anthony/arm-v2-port at the arm-risc0 v2.0.0-rc.6
# pin (crates.io `=2.0.0-rc.6`; anomapay-solana-resource 963d6cb, resource
# logic ref dc1c0b4a…). The adapter and forwarder program sources are those
# of the reviewed anthony/anomapay-resource-v2 line as merged (#84); the
# adapter IDL is unchanged and idl/spl_token_forwarder.json is regenerated
# (one error message). The devnet adapter 28Hvr1YFv2ouGN2fS99aF3ZzYXzkncJVVaHcZNhquLFT
# was upgraded to this commit's production build on 2026-09-21.
a27506015377017d008a25ddd4ef6b6d3b6373ea

# solana-protocol-adapter anthony/arm-v2-port with the forwarder's in-place
# logic-ref rotation: set_logic_ref (upgrade authority) and the LogicRefSet
# event. idl/spl_token_forwarder.json is regenerated from this commit; the
# adapter IDL is unchanged. The rotation is an operator instruction driven by
# the adapter repository's ops script, so this package adds no builder for it.
91b72c840112fe67d079e91926cf477848086282
